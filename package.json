{
  "name": "fivemservermingo",
  "version": "1.0.0",
  "description": "FiveM server configuration and custom resources",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
